query getUsers {
  getUsers {
    id
    email
  }
}

query getUserById($id: ID!) {
  getUserById(id: $id) {
    id
    email
  }
}

mutation addUser($userInput: UserInput!) {
  addUser(userInput: $userInput) {
    id
    email
  }
}
